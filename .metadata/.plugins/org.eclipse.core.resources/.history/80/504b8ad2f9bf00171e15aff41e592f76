#include <iostream>
#include <cstdlib>


#include "List.h"

int main(int argc, char const *argv[])
{
	//Here we create a new Linked List
    LinkedList* list = new LinkedList();

    //We populate the list with
    for (int i = 0; i < 100; ++i)
    {
        list->add(rand() % 100);
    }

    list->print();
    std::cout << "List Length: " << list->length << std::endl;
    delete list;
    return 0;
}
